User is the main input in our system because he/she is the one who is deciding what to do.
The whole processing is done by our mobile application. In this stage if the user chooses to use a VR box to experience a better view then the processing will change from the normal processing.
Output is given for the user in VR and AR technologies according to the user choice. But when user chose a VR box to make the view then the output will be deliver to the VR box and its joystick.
